# 下轮实测清单（公司机执行）

> 目标：验证 QA 误报已消除 → 用 QA 报告正式评估 85% 商用率 → 决定是否进入 V9.2。
> 前提：画布项目 58d2b7b2 的 QA 卡已热更新为「按内容认图」版；仓库 V9.1定制版8分镜.lxf 已核验与蓝图零漂移（2026-07-08 家机核验）。

## 第 1 步：只重跑组④ QA（不重新生图，省钱）
画布里 8 张 G 卡成品还在 → 直接单独 run QA 卡即可，**不要重跑组③**。

检查 QA 报告三点：
- [ ] STRUCTURE_CHECK 无 "Set X 缺失/重复"（上轮6个误报应消失）
- [ ] SCORE_TABLE 的「构图识别」描述与实际图逐张对得上（例：G_2 那行写的确实是背面全身那张）
- [ ] HARD_FAILS_BY_REF 只剩真实问题（若仍有硬失败，人工核对该图确认是真失败还是新误报）

## 第 2 步：85% 商用率评估口径
- 评分 ≥7 的张数 ÷ 8。目标 ≥ 7/8（87.5%）。
- 版型专项：8 张「版型合身度」列全部达标且松紧一致，才算紧身硬指令链完全成功。
- 记录 BEST_IMAGE（主图候选）与 WEAKEST_IMAGE。

## 第 3 步：按结果分流
| 结果 | 动作 |
|---|---|
| ≥7/8 可用，无真实硬失败 | ✅ 达标 → 跑组⑥出详情页素材，V9.1 定版；开 V9.2 规划（批量SKU循环 / QA自动触发返修） |
| 个别图失败（1-2张） | 跑组⑤返修支线：把 QA 的 REPAIR_BRIEF 给 R 卡 → RG 单卡重跑，替换后复检 |
| 紧身感仍系统性不足 | 执行备选方案（下方），改蓝图重建，不要在画布上手改 |

## 备选方案：上身效果参考图（紧身感不足时才做）
1. IN3 细节图槽位改放一张「紧身上身效果参考图」（其他款的紧身实拍也行）。
2. 蓝图改动（铁律2，只动 blueprints）：`prompts/A1_商品DNA.txt` 增加读取规则——"IN3 若为上身照，其松紧状态作为字段4的视觉证据，与需求卡②互相印证"。
3. `solutions/build_lxf.py` 重建 .lxf → 核验 → **新建项目** apply（结构没变的话也可只对 A1 做 content-only 单卡热更新）。

## 计费提醒
- 只跑 QA ≈ 1 次 LLM 调用（12图输入，便宜）。
- 组③全量重跑 = 8 张 high 质量图并发，瞬时消耗大——除非成品图丢失，否则本轮不需要。
